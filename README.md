# EMS (Employee Management System)

Simple React + Vite app with admin and employee dashboards.

Run locally:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```

License: (add license info)
